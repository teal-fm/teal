{
  "name": "@repo/aqua",
  "private": true,
  "scripts": {
    "install": ":",
    "build": "SQLX_OFFLINE=true cargo build --release --manifest-path ../../Cargo.toml -p aqua",
    "build:rust": "SQLX_OFFLINE=true cargo build --release --manifest-path ../../Cargo.toml -p aqua",
    "dev": "cargo watch -x 'run -p aqua'",
    "test": "SQLX_OFFLINE=true cargo test -p aqua",
    "test:rust": "SQLX_OFFLINE=true cargo test -p aqua"
  },
  "packageManager": "pnpm@10.18.0",
  "dependencies": {
    "jest-expo": "^54.0.12"
  }
}
