# Teal marketing site

The standalone landing page for Teal, built with vinext and deployed through
Sites. It presents Teal as an open, social home for music and links visitors to
the live app at `https://sigilyph.teal.fm`.

## Development

```bash
npm install
npm run dev
```

Run `npm run lint`, `npm run test`, and `npm run build` before publishing.
