{
  "name": "@repo/rocketman",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "build:rust": "cargo build --release",
    "dev": "cargo watch -x 'run'",
    "test": "cargo test",
    "test:rust": "cargo test"
  }
}