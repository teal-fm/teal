{
  "name": "@repo/satellite",
  "private": true,
  "scripts": {
    "install": ":",
    "build": "SQLX_OFFLINE=true cargo build --release --manifest-path ../../Cargo.toml -p satellite",
    "build:rust": "SQLX_OFFLINE=true cargo build --release --manifest-path ../../Cargo.toml -p satellite",
    "dev": "cargo watch -x 'run -p satellite'",
    "test": "SQLX_OFFLINE=true cargo test -p satellite",
    "test:rust": "SQLX_OFFLINE=true cargo test -p satellite"
  },
  "packageManager": "pnpm@11.5.2"
}
