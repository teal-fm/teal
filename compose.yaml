services:
  aqua-api:
    build:
      context: .
      dockerfile: apps/aqua/Dockerfile
    container_name: aqua-app
    ports:
      - "3000:3000"
    networks:
      - app_network
    depends_on:
      - postgres
      - garnet
    env_file:
      - .env
