{
  "name": "teal",
  "private": true,
  "version": "0.0.0",
  "packageManager": "bun@1.0.0+",
  "scripts": {
    "install": "turbo run install",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
